<%= form_with(model: status, local: true) do |form| %>
  <% if status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :submission_status %>
    <%= form.select :submission_status, ['Hired', 'In_Process', 'Not_Seleceted', 'Received', 'Submitted'], id: :status_submission_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
